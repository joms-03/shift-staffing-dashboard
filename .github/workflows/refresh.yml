name: Refresh dashboard from Google Sheet

on:
  schedule:
    - cron: "5 * * * *"
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: refresh-dashboard
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Refresh index.html from the sheet
        run: python3 scripts/refresh_dashboard.py

      - name: Commit and push if changed
        run: |
          git config user.name "shift-dashboard-bot"
          git config user.email "actions@users.noreply.github.com"
          if git diff --quiet -- index.html; then
            echo "No data changes."
            exit 0
          fi
          git add index.html
          git commit -m "Auto-refresh dashboard data from sheet"
          git push
